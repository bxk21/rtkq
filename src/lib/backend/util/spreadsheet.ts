// import { GoogleSpreadsheetRow } from "google-spreadsheet";

// export function get<ReturnType, RowType = any>(row: GoogleSpreadsheetRow<Partial<RowType>>, key: keyof RowType): ReturnType {
// 	if ()
// 	row.get(key)
// }